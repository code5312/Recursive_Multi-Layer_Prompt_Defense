2025-11-xx Recursive Ablation (Placeholder)
===========================================

This directory is reserved for storing the recursive ablation experiment
artifacts (configuration snapshots, metrics, and analysis notes) once the run
is completed.

Action items:

- Move the generated `results.json`, charts, and supporting notebooks here
  after executing the ablation pipeline.
- Remove this placeholder file if you export the experiment to a separate
  tracking service (e.g. Weights & Biases) instead of keeping artifacts locally.

