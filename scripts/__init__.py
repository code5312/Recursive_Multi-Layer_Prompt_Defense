"""
Utility namespace marker for script entrypoints.

Having this module (even with a minimal docstring) lets us import helpers
from `scripts` in other parts of the codebase without Python treating the
directory as plain data.
"""
